---
    to: <%=appName%>/www/fn/debounce.js
---
export default function debounce (fn, wait) {
    let t
    return function () {
        clearTimeout(t)
        t = setTimeout(() => fn.apply(this, arguments), wait)
    }
}

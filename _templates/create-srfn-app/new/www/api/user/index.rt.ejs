---
    to: "<%=authEnabled && rolesEnabled ? `${appName}/www/api/user/index.rt.js` : null%>"
---
const { userdb } = require( '../../../userdb.js' )
module.exports = {
    POST: async( { req, body } ) => userdb.saveUser( body )
}

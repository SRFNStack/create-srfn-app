---
    to: "<%=userApiEnabled && rolesEnabled ? `${appName}/www/api/user/index.rt.js` : null%>"
---
const { userdb } = require( '../../../db/userdb.js' )
module.exports = {
    POST: async( { req, body } ) => userdb.saveUser( body )
}

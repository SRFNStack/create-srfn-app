---
    to: "<%=authEnabled ? `${appName}/www/api/user/index.rt.js` : null%>"
---
const { saveUser } = require( '../../../lib/userdb.js' )
module.exports = {
    POST: async( { body } ) => saveUser( body )
}
